apply plugin: "java"

dependencies {

    compile(
            [group: "org.jsoup", name: "jsoup", version: jsoup_version],
            [group: "org.apache.httpcomponents", name: "httpclient", version: commons_httpcomponents_version]
    )

}
